server:
  port: 9010
  servlet:
    context-path: /tenant
spring:
  application:
    name: tenant-service
  profiles:
    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/